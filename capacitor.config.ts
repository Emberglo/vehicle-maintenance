import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'vehicle-maintenance',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
